# Where enquiries go. Every form on the site (customer, dealer, contact) posts
# to /api/enquiry, which forwards the enquiry as JSON to this URL.
#
# Point it at an inbox relay, a CRM intake endpoint, or a Zapier/Make hook.
# Unset: accepted and logged in development, 503 in production.
ZAP_ENQUIRY_WEBHOOK=
